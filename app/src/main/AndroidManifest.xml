<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hello1application">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".medical.medicalRecord.TestActivity"></activity>
        <activity android:name=".medical.medicalRecord.calculateDosage.TestActivity" />
        <activity android:name=".medical.medicalRecord.calculateDosage.DosageReviewActivity" />
        <activity android:name=".medical.medicalRecord.PreMedicalActivity" />
        <activity android:name=".test.listenerAndCallback.EventActivity1" />
        <activity android:name=".common.pay.OrderDetailsViewActivity" />
        <activity android:name=".test.AActivity" />
        <activity android:name=".medical.quota.reportDetail.RecordIndexDetailActivity" />
        <activity
            android:name=".common.login.ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".medical.quota.reportDetail.IndexDetailsActivity" />
        <activity
            android:name=".common.FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".BottomActivity"
            android:label="@string/title_activity_bottom" />
        <activity android:name=".common.PickTimeActivity" />
        <activity android:name=".medical.BasicInfoActivity" />
        <activity android:name=".medical.medicalRecord.record.PostMedicalRecordActivity" />
        <activity android:name=".common.DialogActivity" />
        <activity android:name=".medical.quota.ruleViewDemo.RuleViewActivity" />
        <activity android:name=".medical.quota.reportDetail.ReportDetailCollectionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>